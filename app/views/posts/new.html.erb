<%# 
Таким образом, всякий раз, когда вы отправляете запрос GET 
на URL posts /new с атрибутом data-turbo-frame="modal",
вместо выполнения полностраничного перенаправления запрос 
будет извлекать содержимое в turbo_frame_tag внутри posts 
/new.html.erb и отображать его в turbo_frame_tag "модальный" 
в вашем макете на вашей текущей странице. %>
<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New post</h1>
  <%= turbo_frame_tag "modal" do%>
    <%= render "form", post: @post %>
  <% end %>
  <%# Итак, чтобы отправить такой "удаленный" запрос, вы можете изменить свой link_to,
  добавив данные: { turbo_frame: 'modal' }: %>
  <%= link_to 'Back to posts', posts_path, 
    class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium"%>
</div>
